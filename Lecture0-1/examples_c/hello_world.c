#include <stdio.h> //including the library

int main(void) //you need to use this function when your program needs no initial parameters to run/ load (parameters received from the OS - out of the program it self).
{
    printf("Hello World");
}