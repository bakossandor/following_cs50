#include <stdio.h>

int counter = 1;
int main(void)
{
    if (counter > 0) 
    {
        printf("counter is more than zero");
    } else if (counter == 0) {
        printf("counter is zero");
    } else {
        printf("counter is more than zero");
    }
}